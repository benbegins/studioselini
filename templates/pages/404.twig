{% extends "layout.twig" %}

{% block content %}
    
    <section class="pb-section lg:pt-16">
        <div class="container flex flex-col items-center text-center">
            <div>
                <img src="{{theme.uri}}/src/images/404.png" alt="404">
            </div>
            <h1 class="text-body-xl font-titling-subhead mb-6 leading-tight">Ooooh... Il semblerait que cette page n’existe pas.</h1>
            <p class="mb-8 lg:w-1/2">Inspirez profondément et en pleine conscience, et utilisez notre navigation pour explorer nos cours, nos ateliers et le studio. Namaste !</p>
            {% include "components/btns.twig" with { primary: true, text: "Retournez à l'acceuil", url: site.url } %}
        </div>

        {# BG Gradient #}
        <div class="absolute opacity-50 -z-10 w-[250vw] h-[250vw] top-1/2 left-0 -translate-x-1/3 -translate-y-1/2 lg:w-[100vw] lg:h-[100vw]" style="background: radial-gradient(50% 50% at 50% 50%, #BFA597 0%, rgba(191, 165, 151, 0.00) 100%);">
        </div>
    </section>

{% endblock %}
